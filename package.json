{
  "name": "leitor-notas-fiscais",
  "version": "1.0.0",
  "private": true,
  "description": "Monitor de e-mails do Gmail para identificar e baixar documentos fiscais.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "authorize": "node src/scripts/authorizeGmail.js",
    "check": "find src test -name '*.js' -print0 | xargs -0 -n1 node --check",
    "test": "node --test"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "dotenv": "^16.4.7",
    "fast-xml-parser": "^5.0.8",
    "googleapis": "^174.0.0",
    "node-cron": "^4.6.0"
  }
}
